##############################################################################
#   Integration-test weewx.conf - SQLite path.
#
#   Mirror of e2e-tests/configs/mariadb/weewx.conf with only the
#   [Databases] block swapped to SQLite. Same [StdReport], [Engine],
#   [MarineDataService], and [LoopData] shape so the two backends
#   exercise the same extension surface. Upstream's marine DDL uses
#   MySQL-only prefix-index syntax; the marine-sqlite-compat patch
#   under patches/extensions/ rewrites the three _create_*_table
#   helpers to emit portable SQL that runs on both backends.
##############################################################################

WEEWX_ROOT = /opt/weewx-data

[Station]
    location     = "ha-weewx sqlite test"
    latitude     = 37.82
    longitude    = -122.48
    altitude     = 0, meter
    station_type = Simulator

[Simulator]
    driver        = weewx.drivers.simulator
    mode          = simulator
    loop_interval = 2.0

[StdReport]
    SKIN_ROOT    = skins
    HTML_ROOT    = /config/www
    data_binding = wx_binding

    [[SeasonsReport]]
        skin   = Seasons
        enable = True

    [[LoopDataReport]]
        skin   = LoopData
        enable = True

    [[CelestialReport]]
        skin      = Celestial
        HTML_ROOT = /config/www/celestial
        enable    = True
        [[[Extras]]]
            loop_data_file = /loop-data.txt

[StdConvert]
    target_unit = METRICWX

[StdCalibrate]
    [[Corrections]]

[StdQC]
    [[MinMax]]
        barometer   = 26, 32.5, inHg
        outTemp     = -40, 120, degree_F
        inTemp      = 10, 120, degree_F
        outHumidity = 0, 100
        inHumidity  = 0, 100
        windSpeed   = 0, 120, mile_per_hour
        rainRate    = 0, 10, inch_per_hour

[StdArchive]
    # Short interval so the test sees an archive row without a long wait.
    # 5 s is the minimum that StdWXCalculate's running-aggregate logic stays
    # comfortable with on the Simulator driver.
    archive_interval  = 5
    archive_delay     = 3
    no_catchup        = True
    record_generation = software
    data_binding      = wx_binding

[DataBindings]
    [[wx_binding]]
        database   = archive_sqlite
        table_name = archive
        manager    = weewx.manager.DaySummaryManager
        schema     = schemas.wview_extended.schema

[Databases]
    [[archive_sqlite]]
        database_name = /config/db/weewx.sdb
        driver        = weedb.sqlite

[StdRESTful]
    [[StationRegistry]]
        register_this_station = False

# Route loopdata's per-LOOP JSON to tmpfs (matches shipped template).
[LoopData]
    [[FileSpec]]
        loop_data_dir = /dev/shm/weewx
        filename      = loop-data.txt
    [[LoopFrequency]]
        seconds = 2.0

# Marine data - enabled to exercise the init_marine_schema.py shim end-to-end
# against MariaDB (upstream's DDL uses MySQL-only PRIMARY KEY prefix syntax).
[MarineDataService]
    enable = true

[Engine]
    [[Services]]
        prep_services    = weewx.engine.StdTimeSynch
        data_services    = user.wxskyfield.WxSkyfield, user.marine_data.MarineDataService
        process_services = weewx.engine.StdConvert, weewx.engine.StdCalibrate, weewx.engine.StdQC, weewx.wxservices.StdWXCalculate
        xtype_services   = weewx.wxxtypes.StdWXXTypes, weewx.wxxtypes.StdPressureCooker, weewx.wxxtypes.StdRainRater, weewx.wxxtypes.StdDelta
        archive_services = weewx.engine.StdArchive
        restful_services = weewx.restx.StdStationRegistry
        report_services  = weewx.engine.StdPrint, weewx.engine.StdReport, user.loopdata.LoopData

[Logging]
    version = 1
    disable_existing_loggers = False

    [[root]]
        level    = INFO
        handlers = console,

    [[formatters]]
        [[[standard]]]
            format = "%(asctime)s  %(name)s %(levelname)s %(message)s"

    [[handlers]]
        [[[console]]]
            class     = logging.StreamHandler
            formatter = standard
            stream    = ext://sys.stdout

    [[loggers]]
        [[[weewx]]]
            level     = INFO
            handlers  = console,
            propagate = False
